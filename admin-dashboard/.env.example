# Copy to ".env" in this folder.
# Since this dashboard runs in a browser on your PC (same machine as the
# backend), "localhost" works fine here — unlike the mobile app, which
# needs your PC's actual network IP.
VITE_API_BASE_URL=http://localhost:5000/api
